---
server: 
  context-path: /auth
  port: 5000

spring:
  profiles: default
  application:
    name: auth-service
    
  client:
    enabled: true  
    serviceUrl: 
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true    
    
management: 
  endpoint:
    health:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
        exclude: 
      base-path: /      
  info:
    git:
      mode: full
  cloudfoundry:
    enabled: true
    skip-ssl-validation: true   
       
logging:
    level:
      ROOT: INFO
      org.springframework.web: DEBUG 
      com.flowers.microservice.auth.service: DEBUG      

        