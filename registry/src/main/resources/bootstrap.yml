---
spring:
  profiles: default
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:passw0rd}
      username: ${CONFIG_SERVICE_USERNAME:user}  

               

