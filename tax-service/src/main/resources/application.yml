server:
  port: ${PORT:5003}

info:
  component: tax.server

logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'

management: 
    metrics: 
      export: 
        influx: 
          db: springboot 
          uri: http://localhost:8086
    endpoint: 
      prometheus: 
        enabled: true    

